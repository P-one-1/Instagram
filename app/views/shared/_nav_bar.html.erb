<!--Navigation Bar-->
<nav class="navbar navbar-light bg-light d-flex">
    <div class="container">
        <div class="logo">
            <a class="navbar-brand" href="#">
                <%= fa_stacked_icon "camera inverse", base: "circle"%>
                Instagram
            </a>
        </div>
        <div class="m-auto">
            <form class="form-inline search-form">
                <input class="form-control mr-sm-2" type="search" placeholder="@search" aria-label="Search">
            </form>
        </div>
        <div class="user-section">
            <a><i class="fa fa-compass"></i></a>
            <a><i class="fa fa-heart"></i></a>
            <% if user_signed_in? %>
                <%= link_to '<i class="fa fa-user"></i>'.html_safe, user_path(current_user) %>
                <%= link_to '<i class="fa fa-sign-out"></i>'.html_safe, destroy_user_session_path, method: :delete %>
            <% else %>
                <%= link_to '<i class="fa fa-sign-in"></i>'.html_safe, new_user_session_path %>
            <% end %>
        </div>
    </div>
</nav>